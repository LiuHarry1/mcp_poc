# 🚀 快速开始 - MCP Server Marketplace

## 30秒快速启动

### Windows 用户

```bash
start_marketplace.bat
```

### macOS/Linux 用户

```bash
chmod +x start_marketplace.sh
./start_marketplace.sh
```

然后访问：**http://localhost:5173**

---

## 📦 体验流程

### 1️⃣ 浏览 Marketplace（0-5分钟）

1. 打开浏览器访问 http://localhost:5173
2. 默认打开 **🏪 Marketplace** 页面
3. 看到：
   - 📊 统计卡片（总包数、下载量等）
   - 🔍 搜索框
   - 📂 分类导航（文件、计算、搜索等）
   - 📦 5个示例包

### 2️⃣ 安装你的第一个 Server（5-10分钟）

**推荐试试：文件操作服务器**

1. 在 Marketplace 中找到 **"文件操作服务器"**
2. 点击卡片查看详情
3. 点击 **安装** 按钮
4. 等待安装完成（会自动跳转到管理器页面）
5. 在管理器页面找到新安装的 Server
6. 点击 **连接** 按钮
7. 看到状态变为 **已连接** ✅

### 3️⃣ 使用 AI 助手调用工具（10-15分钟）

**现在 AI 可以使用文件工具了！**

在聊天框中输入：

```
创建一个文件 test.txt，内容是 "Hello from MCP Marketplace!"
```

AI 会自动：
1. 🔧 调用 `write_file` 工具
2. 📝 写入文件
3. ✅ 返回结果

再试试：

```
读取 test.txt 的内容
```

---

## 🎯 更多体验

### 安装计算工具

```
试试安装 "计算工具服务器"，然后问：
"计算 sqrt(144) + pow(2, 10) 的结果"
```

### 安装 Python 执行器

```
试试安装 "Python 代码执行器"，然后问：
"用 Python 生成一个斐波那契数列前10项"
```

### 组合使用

```
安装多个 Server，然后问：
"生成10个随机数，计算平均值，保存到 result.txt"
```

AI 会自动调用多个工具完成任务！

---

## 📚 发布你自己的 Server

### 方式一：使用 CLI 工具（推荐）

```bash
# 1. 初始化配置
python mcp_publish_tool.py init

# 2. 编辑生成的 mcp_package.json

# 3. 发布
python mcp_publish_tool.py publish
```

### 方式二：复制示例配置

```bash
# 1. 复制示例
cp packages/file-server-package.json packages/my-server-package.json

# 2. 修改配置
# 编辑 packages/my-server-package.json

# 3. 发布
python mcp_publish_tool.py publish --config packages/my-server-package.json
```

---

## 🔍 探索更多

### CLI 命令

```bash
# 查看所有包
python mcp_publish_tool.py list

# 搜索包
python mcp_publish_tool.py list --query 文件

# 查看包详情
python mcp_publish_tool.py info internal/file-server

# 删除包
python mcp_publish_tool.py delete team/old-server
```

### API 文档

访问 http://localhost:9999/docs 查看完整的 REST API 文档。

### 详细文档

阅读 `MARKETPLACE_README.md` 了解更多高级功能。

---

## ❓ 遇到问题？

### 端口被占用

```bash
# 修改 marketplace_server.py 中的端口
# 默认: 9999, 8000, 5173
```

### 服务未启动

```bash
# 手动逐个启动服务进行排查
python marketplace_server.py       # 端口 9999
cd server && python mcp_web_server.py  # 端口 8000
cd client && npm run dev            # 端口 5173
```

### 包未显示

```bash
# 重新初始化数据
python init_marketplace.py
```

---

## 🎉 开始体验吧！

现在你已经准备好了：

1. ✅ 浏览内部 MCP Server Marketplace
2. ✅ 一键安装想要的工具
3. ✅ 使用 AI 智能调用工具
4. ✅ 发布自己的 MCP Server

**Have Fun!** 🚀

